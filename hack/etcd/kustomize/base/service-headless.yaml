apiVersion: v1
kind: Service
metadata:
  name: proxima-etcd-headless
  namespace: proxima-etcd
  labels:
    app: proxima-etcd
spec:
  clusterIP: None
  publishNotReadyAddresses: true
  selector:
    app: proxima-etcd
  ports:
    - name: client
      port: 2379
      targetPort: client
    - name: peer
      port: 2380
      targetPort: peer
